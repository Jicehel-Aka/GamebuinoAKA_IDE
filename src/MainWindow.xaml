<Window x:Class="GamebuinoAKA.IDE.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:GamebuinoAKA.IDE.Views"
        xmlns:vm="clr-namespace:GamebuinoAKA.IDE.ViewModels"
        Title="Gamebuino AKA IDE"
        Height="768" Width="1280"
        MinHeight="600" MinWidth="900"
        WindowStartupLocation="CenterScreen"
        Icon="pack://application:,,,/Assets/logo.png"
        Background="{StaticResource BrushBackground}">

    <Window.DataContext>
        <Binding Path="MainViewModel" Source="{x:Static Application.Current}" />
    </Window.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar Navigation -->
        <Border Grid.Column="0"
                Background="{StaticResource BrushSurface}"
                BorderBrush="{StaticResource BrushBorder}"
                BorderThickness="0,0,1,0">
            <DockPanel>
                <!-- Logo -->
                <Border DockPanel.Dock="Top"
                        Padding="16,20,16,16"
                        BorderBrush="{StaticResource BrushBorder}"
                        BorderThickness="0,0,0,1">
                    <StackPanel Orientation="Horizontal">
                        <Image Source="pack://application:,,,/Assets/logo.png"
                               Width="32" Height="32" Margin="0,0,10,0"
                               VerticalAlignment="Center"
                               RenderOptions.BitmapScalingMode="HighQuality"/>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Gamebuino"
                                       FontSize="18" FontWeight="Bold"
                                       Foreground="{StaticResource BrushAccent}"/>
                            <TextBlock Text="AKA IDE"
                                       FontSize="14"
                                       Foreground="{StaticResource BrushTextMuted}"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Nav Items -->
                <StackPanel DockPanel.Dock="Top" Margin="0,8,0,0">
                    <Button Style="{StaticResource NavButtonStyle}"
                            Command="{Binding NavigateToHomeCommand}"
                            Content="🏠  Accueil"
                            Tag="{Binding CurrentPage}"
                            IsEnabled="{Binding IsNotBusy}"/>
                    <Button Style="{StaticResource NavButtonStyle}"
                            Command="{Binding NavigateToProjectsCommand}"
                            Content="📁  Projets"
                            IsEnabled="{Binding IsNotBusy}"/>
                    <Button Style="{StaticResource NavButtonStyle}"
                            Command="{Binding NavigateToNewProjectCommand}"
                            Content="➕  Nouveau projet"
                            IsEnabled="{Binding IsNotBusy}"/>
                    <Button Style="{StaticResource NavButtonStyle}"
                            Command="{Binding NavigateToSpriteEditorCommand}"
                            Content="🎨  Sprites"
                            IsEnabled="{Binding IsNotBusy}"/>
                    <Button Style="{StaticResource NavButtonStyle}"
                            Command="{Binding NavigateToTilemapEditorCommand}"
                            Content="🗺️  Tilemaps"
                            IsEnabled="{Binding IsNotBusy}"/>
                    <Button Style="{StaticResource NavButtonStyle}"
                            Command="{Binding NavigateToSoundBankCommand}"
                            Content="🎵  Banque sonore"
                            IsEnabled="{Binding IsNotBusy}"/>
                </StackPanel>

                <!-- Bottom: Settings -->
                <Border DockPanel.Dock="Bottom"
                        BorderBrush="{StaticResource BrushBorder}"
                        BorderThickness="0,1,0,0">
                    <Button Style="{StaticResource NavButtonStyle}"
                            Command="{Binding NavigateToSettingsCommand}"
                            Content="⚙️  Paramètres"
                            IsEnabled="{Binding IsNotBusy}"/>
                </Border>
            </DockPanel>
        </Border>

        <!-- Main Content Area -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Page Content -->
            <ContentControl Grid.Row="0"
                            Content="{Binding CurrentPage}"
                            Margin="0"/>

            <!-- Status Bar -->
            <Border Grid.Row="1"
                    Background="{StaticResource BrushAccent}"
                    Padding="12,4">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0"
                               Text="{Binding StatusMessage}"
                               Foreground="White"
                               FontSize="11"
                               VerticalAlignment="Center"/>
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <TextBlock Text="{Binding PlatformIOVersion, StringFormat='PlatformIO: {0}'}"
                                   Foreground="White" FontSize="11" VerticalAlignment="Center"
                                   Margin="0,0,16,0"/>
                        <TextBlock Text="{Binding VSCodePath, StringFormat='VSCode: {0}'}"
                                   Foreground="White" FontSize="11" VerticalAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
